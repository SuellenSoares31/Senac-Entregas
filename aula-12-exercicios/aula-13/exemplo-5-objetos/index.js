
// Criar um objeto sobre uma pessoa
const pessoa = {
    nome: "José",
    pet: {
        nome: "Jack",
        raca: "Dálmata",
        idade: 1,
    },
}

// Imprimir os dados da pessoa
console.log(`O nome da pessoa é ${pessoa.nome}, ela possui um pet chamado ${pessoa.pet.nome}, que é da raça ${pessoa.pet.raca} e que tem ${pessoa.pet.idade} anos`)
